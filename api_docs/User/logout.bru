meta {
  name: logout
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/{{apiExt}}/logout/
  body: multipartForm
  auth: inherit
}

headers {
  Accept: application/json
}

body:multipart-form {
  refresh_token: {{refreshToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
