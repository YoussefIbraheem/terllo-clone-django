meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{apiExt}}/login/
  body: multipartForm
  auth: inherit
}

headers {
  Accept: application/json
}

body:multipart-form {
  email: test@test.com
  password: password@123
}

script:pre-request {
  
  
}

script:post-response {
  let token = res.body["tokens"]["access"]
  bru.setEnvVar("accessToken", token)
}

settings {
  encodeUrl: true
  timeout: 0
}
