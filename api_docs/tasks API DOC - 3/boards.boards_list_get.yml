info:
  name: boards.boards_list_get
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/boards/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{url}}/boards/"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
