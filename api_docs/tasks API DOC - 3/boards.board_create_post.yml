info:
  name: boards.board_create_post
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/boards/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{url}}/boards/"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
