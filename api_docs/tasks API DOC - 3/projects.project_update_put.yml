info:
  name: projects.project_update_put
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/projects/:project_id"
  params:
    - name: project_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{url}}/projects/:project_id"
      method: PUT
      params:
        - name: project_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
