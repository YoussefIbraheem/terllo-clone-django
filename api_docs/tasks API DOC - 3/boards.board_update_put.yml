info:
  name: boards.board_update_put
  type: http
  seq: 9

http:
  method: PUT
  url: "{{url}}/boards/:board_id"
  params:
    - name: board_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{url}}/boards/:board_id"
      method: PUT
      params:
        - name: board_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
