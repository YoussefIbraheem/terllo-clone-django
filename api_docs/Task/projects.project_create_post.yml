info:
  name: projects.project_create_post
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/projects/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/projects/"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
