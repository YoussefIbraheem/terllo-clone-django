info:
  name: projects.projects_list_get
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/projects/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/projects/"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
